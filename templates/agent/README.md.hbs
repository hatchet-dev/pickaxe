# {{pascalCase name}} Agent

{{description}}

## Usage

```typescript
import { {{pascalCase name}} } from './{{kebabCase name}}.agent';

// Create agent instance
const agent = new {{pascalCase name}}();

// Execute agent with input
const result = await agent.execute('your input here');
console.log(result);
```

## Properties

- **Name**: `{{name}}`
- **Description**: `{{description}}`

## Methods

### `execute(input: string): Promise<string>`

Processes the given input and returns a result.

**Parameters:**
- `input` - The input string to process

**Returns:**
- A promise that resolves to the processed result string

## Testing

Run the agent tests:

```bash
npm test {{kebabCase name}}.test.ts
```

## Generated Files

This agent was generated with the following files:

- `{{kebabCase name}}.agent.ts` - Main agent implementation
- `{{kebabCase name}}.test.ts` - Test suite
- `README.md` - This documentation

---

**Created**: {{currentDate}}  
**Generator**: Pickaxe CLI