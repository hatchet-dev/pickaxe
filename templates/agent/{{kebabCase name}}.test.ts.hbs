import { {{pascalCase name}} } from './{{kebabCase name}}.agent';

describe('{{pascalCase name}}', () => {
  let agent: {{pascalCase name}};

  beforeEach(() => {
    agent = new {{pascalCase name}}();
  });

  describe('constructor', () => {
    it('should create agent with correct name and description', () => {
      expect(agent.name).toBe('{{name}}');
      expect(agent.description).toBe('{{description}}');
    });
  });

  describe('execute', () => {
    it('should execute and return processed input', async () => {
      const input = 'test input';
      const result = await agent.execute(input);

      expect(result).toBe('{{pascalCase name}} processed: test input');
    });

    it('should handle empty input', async () => {
      const result = await agent.execute('');

      expect(result).toBe('{{pascalCase name}} processed: ');
    });
  });
});